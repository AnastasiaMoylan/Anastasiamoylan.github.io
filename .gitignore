node_modules

# Build output — regenerated by CI on deploy (see .github/workflows/deploy.yml)
dist
dist-server

# macOS Finder metadata
.DS_Store

# Claude Code guidance & local session config — local only
CLAUDE.md
.claude/
AGENT.md
DECISIONS.md

# Local working docs — case-study inputs, source notes, positioning briefs; not part of the published site
docs/
instructions/

# Built renderings embed the screenshots; the templates are the source
renderings/*.html
!renderings/*.src.html
