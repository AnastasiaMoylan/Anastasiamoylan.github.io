node_modules

# Build output — regenerated by CI on deploy (see .github/workflows/deploy.yml)
dist
dist-server

# macOS Finder metadata
.DS_Store

# Claude Code guidance & local session config — local only
CLAUDE.md
.claude/

# Authoring scaffolding — kept locally, not part of the published portfolio
docs/
instructions/
